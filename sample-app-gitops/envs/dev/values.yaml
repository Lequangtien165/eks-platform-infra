replicaCount: 2

image:
  repository: __ECR_REPOSITORY_URI__
  tag: "12fe1504536b"
  pullPolicy: IfNotPresent

env:
  appName: sample-app
  appEnv: dev
  appVersion: "0.1.1"

service:
  port: 80
  targetPort: 8000

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: sample-app-dev.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
